import { Router } from "express";
import mock from "../mock.json";

const router = Router();
//rota para criar pacientes novos conversando com a database

router.put("/", async (req, res) => {});

export default router;
